[project]
name = "llm-pkm-harness-poc"
version = "0.2.0"
description = "Proof of concept for LLM PKM harness with Ollama ingest, embeddings, and query"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "ipykernel>=6.29",
    "jupyter>=1.0",
    "numpy>=1.26",
]
